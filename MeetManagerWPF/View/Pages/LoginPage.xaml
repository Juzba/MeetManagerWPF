<Page x:Class="MeetManagerWPF.View.Pages.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:UserControls="clr-namespace:MeetManagerWPF.View.UserControls"
      xmlns:ViewModel="clr-namespace:MeetManagerWPF.ViewModel"
      xmlns:local="clr-namespace:MeetManagerWPF.View.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="LoginPage">
    <!--<Page.DataContext>
        <ViewModel:LoginViewModel/>
    </Page.DataContext>-->

    <Grid Background="#20448C">

        <Grid.RowDefinitions>
            <RowDefinition Height="100"></RowDefinition>
            <RowDefinition Height="300"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition MinWidth="330"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.ColumnSpan="3" TextAlignment="Center" Margin="0 0 0 15" FontSize="60" Text="Login" Foreground="#FFBC24" ></TextBlock>
       
        <StackPanel Grid.Column="1" Grid.Row="1"  HorizontalAlignment="Center" VerticalAlignment="Center">
            <UserControls:Input
                Placeholder="Jméno"
                PlaceholderColor="#20448C"
                Background="#008CDB"
                Foreground="#FFD340"
                Margin="10"
                ></UserControls:Input>
            <UserControls:Input
                Placeholder="Heslo"
                PlaceholderColor="#20448C"
                Background="#008CDB"
                Foreground="#FFD340"
                Margin="10"
            ></UserControls:Input>
            <Button 
                Background="#FFBC24" 
                Foreground="#20448C" 
                Margin="20" 
                Width="200" 
                FontSize="30" 
                Content="Přihlásit" >
            </Button>

            <Button 
                Background="Transparent" 
                BorderBrush="Transparent"
                Width="200"
                Command="{Binding NavigateToRegisterCommand}"
                Content="Registrovat"
                FontSize="20"
                >
            </Button>
        </StackPanel>
    </Grid>
</Page>
